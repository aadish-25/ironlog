services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: ironlog
      POSTGRES_PASSWORD: ironlog
      POSTGRES_DB: ironlog_dev
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
volumes:
  postgres_data: